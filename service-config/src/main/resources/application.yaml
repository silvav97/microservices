server:
  port: 8081  
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/silvav97/microservices-tutorial
          search-paths: config-data
  application:
    name: service-config