server:
  port: 8081  
spring:
  cloud:
    config:
      server:
        git:
          default-label: zipkin-sleuth
          uri: https://github.com/silvav97/microservices
          search-paths: config-data
  application:
    name: service-config